import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'i6-ng15-play',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
